<h1>Items</h1>

<div id="items">
  <% @items.each do |item| %>
    <%= render item %>
    <p>
      <%= link_to "Show this item", collection_item_path(@collection, item) %>
    </p>
  <% end %>
</div>

<%= link_to "New item", new_collection_item_path(@collection) %>
