<!DOCTYPE html>
<html lang="<%= session[:language] || 'en' %>">
<head>
  <title>Collector</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <%= javascript_importmap_tags %>


  <!-- CSS -->
  <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" %>

  <!-- JS -->
  <%= javascript_include_tag "https://code.jquery.com/jquery-3.7.1.min.js" %>
</head>

<body>
<% if session[:theme] == 'dark' %>
  <nav class="navbar navbar-expand-sm bg-primary navbar-dark bg-dark">
<% else %>
  <nav class="navbar navbar-expand-sm bg-primary navbar-dark">
<% end %>
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><%= session[:language] == 'english' ? 'Collector' : 'কালেক্টর' %></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="collapsibleNavbar">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <%= link_to session[:language] == 'english' ? "Home" : "হোম", root_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to session[:language] == 'english' ? "Users list" : "ইউজার্স লিস্ট", "#", class: "nav-link" %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"><%= session[:language] == 'english' ? "Collections" : "কালেকশন্স" %></a>
          <ul class="dropdown-menu">
            <li><%= link_to session[:language] == 'english' ? "Your Collections" : "আপনার কালেকশন্স", collections_path, class: "dropdown-item" %></li>
            <li><%= link_to session[:language] == 'english' ? "Create new collection" : "নতুন তৈরী করুন", new_collection_path, class: "dropdown-item" %></li>
          </ul>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"><%= session[:language] == 'english' ? "Theme" : "থিম" %></a>
            <ul class="dropdown-menu">
              <li class="nav-item">
                <%= button_to session[:language] == 'english' ? "Light" : "লাইট", update_preferences_path(theme: 'light'), method: :patch, class: "dropdown-item" %>
              </li>
              <li class="nav-item">
                <%= button_to session[:language] == 'english' ? "Dark" : "ডার্ক", update_preferences_path(theme: 'dark'), method: :patch, class: "dropdown-item" %>
              </li>
            </ul>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">Language</a>
            <ul class="dropdown-menu">
              <li class="nav-item">
                <%= button_to "English", update_preferences_path(language: 'english'), method: :patch, class: "dropdown-item" %>
              </li>
              <li class="nav-item">
                <%= button_to "Bengali", update_preferences_path(language: 'bengali'), method: :patch, class: "dropdown-item" %>
              </li>
            </ul>
          </li>
          <li class="nav-item">
            <%= link_to session[:language] == 'english' ? "Edit profile" : "প্রোফাইল আপডেট", edit_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= button_to session[:language] == 'english' ? "Logout" : "লগআউট", destroy_user_session_path, method: :delete, class: "nav-link", form: { class: "d-inline" } %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

<%= yield %>

<%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" %>
</body>
</html>
